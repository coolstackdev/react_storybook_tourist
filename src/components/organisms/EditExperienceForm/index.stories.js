import React from 'react'
import { storiesOf } from '@storybook/react'
import { EditExperienceForm } from 'components'

storiesOf('EditExperienceForm', module)
	.add('default', () => (
		<EditExperienceForm />
	))
